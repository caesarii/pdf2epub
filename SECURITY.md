# Security Policy

Do not commit real API keys, PDFs, OCR outputs, or generated EPUB files. The repository `.gitignore` excludes common local input/output paths, but review changes before publishing.

If you find a vulnerability or accidental secret exposure, rotate the affected key immediately and remove it from Git history before making the repository public.
